apiVersion: v1
kind: Service
metadata:
  name: "flux-2048"
  namespace: "fluxdemo"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
  selector:
    app: "2048"
